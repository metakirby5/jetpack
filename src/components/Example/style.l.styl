// By convention, styles go in components/component_name/style.l.styl.
// These styles are locally scoped, so use camelCase and import them.

.exampleContainer
  display flex
  flex-direction column
  justify-content center
  align-items center
  width 100%
  height 100%

.exampleMain
  height 90%
  overflow auto
