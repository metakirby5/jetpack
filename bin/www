#!/usr/bin/env node
// The main server executable. You shouldn't touch this.
'use strict';

const config = require('../config');
const port = process.env.PORT || config.ports.server;

require('coffee-script/register');
require('../server').listen(port, (err, result) => {
  console.log(err || `Listening at ${port}...`);
});
